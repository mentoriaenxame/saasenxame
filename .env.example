# Environment variables for local development and production deployment

# Next.js Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# PostgreSQL Database Configuration
DB_USER=postgres
DB_HOST=localhost
DB_NAME=crm_db
DB_PASSWORD=your_password
DB_PORT=5432

# SSL Configuration for remote database connections (set to 'true' or 'false')
DB_SSL=false